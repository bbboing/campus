<view class="feature-container">
    <van-button
        round
        type="info"
        custom-class="feature-button"
        wx:for="{{list}}"
        id="{{index}}">
            {{item.title}}
    </van-button>
</view>